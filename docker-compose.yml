services:
  piano:
    build: .
    volumes:
      - ./data:/data
      - model_cache:/root/piano_transcription_inference_data
    entrypoint: ["python", "/app/txt-format.py"]

volumes:
  model_cache:
